@*@model CF_Budgeter.Models.Household

@{
    ViewBag.Title = "Join";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="row">
    <div class="col-md-3">

    </div>
    <div class="col-md-6 tile">
        <h3>Are you sure you want to leave @Model.Name?</h3>
        <div>

            @using (Html.BeginForm("Create", "Households", new { id = Model.Id }, FormMethod.Post))
            {
                @Html.AntiForgeryToken()
                @Html.HiddenFor(model => model.HouseholdId)
                @Html.HiddenFor(model => model.HouseholdName)
                <div class="form-actions no-color">
                    <input type="submit" value="LEAVE" class="btn btn-block btn-primary" />
                    @Html.ActionLink("Cancel", "Index", "Home", null, new { @class = "btn btn-block btn-primary" })
                </div>
            }
        </div>
        <br />
    </div>
    <div class="col-md-3">

    </div>
</div>*@